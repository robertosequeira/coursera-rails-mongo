<%= form_for(@api_racer) do |f| %>
  <% if @api_racer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_racer.errors.count, "error") %> prohibited this api_racer from being saved:</h2>

      <ul>
      <% @api_racer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
